<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Post</title>
  <style>
    body {
  font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, #e0e7ff 0%, #f4f6f9 100%);
  margin: 0;
  padding: 40px 20px;
  color: #333;
  display: flex;
  justify-content: center;
  align-items: flex-start;
  min-height: 100vh;
}

.edit-container {
  background: white;
  padding: 36px 48px;
  border-radius: 16px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.07);
  max-width: 600px;
  width: 100%;
  box-sizing: border-box; /* Added for better padding and width handling */
}

h2 {
  color: #34495e;
  margin-bottom: 24px;
  font-weight: 700;
  user-select: none;
}

textarea {
  width: 100%;
  min-height: 160px;
  border: 2px solid #d1d5db;
  border-radius: 12px;
  padding: 18px 20px;
  font-size: 1.1rem;
  resize: vertical;
  margin-bottom: 28px;
  font-family: inherit;
  transition: border-color 0.3s ease, box-shadow 0.3s ease;
  box-sizing: border-box; /* Added for padding inside width */
}

textarea:focus {
  border-color: #4f46e5;
  outline: none;
  box-shadow: 0 0 10px #c7d2fe;
}

button[type="submit"] {
  background-color: #dbca34; /* Yellow gold */
  color: white;
  border: none;
  padding: 14px 32px;
  border-radius: 10px;
  font-size: 1.1rem;
  font-weight: 600;
  cursor: pointer;
  box-shadow: 0 6px 12px rgba(219, 202, 52, 0.6);
  transition: background-color 0.3s ease, box-shadow 0.3s ease;
  user-select: none;
  display: inline-block;
  text-align: center;
  text-decoration: none;
}

button[type="submit"]:hover,
button[type="submit"]:focus {
  background-color: #c7b22e;
  box-shadow: 0 8px 16px rgba(199, 178, 46, 0.8);
  outline: none;
}

  </style>
</head>
<body>
  <div class="edit-container">
    <h2>Edit Your Post</h2>
    <form action="/edit/<%= post._id %>" method="POST">
      <textarea name="postData" required><%= post.postData %></textarea>
      <button type="submit">Update</button>
    </form>
  </div>
</body>
</html>
